
# AI-driven Website Personalization Tool

## Getting Started

1. Install dependencies by running `pip install -r requirements.txt`
2. Run the application by executing `python app.py`
3. Access the homepage at `http://localhost:5000`
