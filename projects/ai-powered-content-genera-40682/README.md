markdown
# AI-powered Content Generation Tool

Automatically generate high-quality content for blogs and social media.

## Running the Application

1. Install dependencies by running `pip install -r requirements.txt`
2. Run the application using `gunicorn web:app`


Note: To run the application, navigate to the project directory and use `gunicorn` instead of `python app.py` to ensure proper deployment.