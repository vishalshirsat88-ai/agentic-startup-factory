
# Virtual Event Promotion Platform

## Instructions

1. Install dependencies by running `pip install -r requirements.txt`
2. Start the server by running `gunicorn app:app`
3. Alternatively, you can use the Railway CLI to deploy the application by running `railway up`
4. Access the application at `http://localhost:5000` or the URL provided by Railway
