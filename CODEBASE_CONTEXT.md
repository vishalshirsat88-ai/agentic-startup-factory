# Codebase Context Snapshot
Generated: 2026-03-18 11:19:17.476969+00:00

## Project Structure


### Folder: .
- PROJECT_CONTEXT.md
- build_saas_engine.py
- generate_context.py
- CODEBASE_CONTEXT.md
- fill_repo.py
- README.md

### Folder: ./memory
- experiments.md
- decisions.md
- learnings.md

### Folder: ./engine
- template_renderer.py

### Folder: ./logs

### Folder: ./saas_master_template
- app.py

### Folder: ./saas_master_template/templates

### Folder: ./saas_master_template/static

### Folder: ./saas_master_template/static/assets

### Folder: ./saas_master_template/static/assets/css

### Folder: ./saas_master_template/static/assets/scss

### Folder: ./saas_master_template/static/assets/img

### Folder: ./saas_master_template/static/assets/img/portfolio

### Folder: ./saas_master_template/static/assets/img/clients

### Folder: ./saas_master_template/static/assets/img/blog

### Folder: ./saas_master_template/static/assets/img/testimonials

### Folder: ./saas_master_template/static/assets/img/team

### Folder: ./saas_master_template/static/assets/js

### Folder: ./saas_master_template/static/assets/vendor

### Folder: ./saas_master_template/static/assets/vendor/glightbox

### Folder: ./saas_master_template/static/assets/vendor/glightbox/css

### Folder: ./saas_master_template/static/assets/vendor/glightbox/js

### Folder: ./saas_master_template/static/assets/vendor/bootstrap-icons

### Folder: ./saas_master_template/static/assets/vendor/bootstrap-icons/fonts

### Folder: ./saas_master_template/static/assets/vendor/swiper

### Folder: ./saas_master_template/static/assets/vendor/bootstrap

### Folder: ./saas_master_template/static/assets/vendor/bootstrap/css

### Folder: ./saas_master_template/static/assets/vendor/bootstrap/js

### Folder: ./saas_master_template/static/assets/vendor/php-email-form

### Folder: ./saas_master_template/static/assets/vendor/isotope-layout

### Folder: ./saas_master_template/static/assets/vendor/purecounter

### Folder: ./saas_master_template/static/assets/vendor/aos

### Folder: ./saas_master_template/static/assets/vendor/imagesloaded

### Folder: ./saas_master_template/static/css

### Folder: ./saas_master_template/static/sneat

### Folder: ./saas_master_template/static/sneat/assets

### Folder: ./saas_master_template/static/sneat/assets/css

### Folder: ./saas_master_template/static/sneat/assets/img

### Folder: ./saas_master_template/static/sneat/assets/img/elements

### Folder: ./saas_master_template/static/sneat/assets/img/icons

### Folder: ./saas_master_template/static/sneat/assets/img/icons/brands

### Folder: ./saas_master_template/static/sneat/assets/img/icons/unicons

### Folder: ./saas_master_template/static/sneat/assets/img/layouts

### Folder: ./saas_master_template/static/sneat/assets/img/illustrations

### Folder: ./saas_master_template/static/sneat/assets/img/avatars

### Folder: ./saas_master_template/static/sneat/assets/img/favicon

### Folder: ./saas_master_template/static/sneat/assets/img/backgrounds

### Folder: ./saas_master_template/static/sneat/assets/js

### Folder: ./saas_master_template/static/sneat/assets/vendor

### Folder: ./saas_master_template/static/sneat/assets/vendor/css

### Folder: ./saas_master_template/static/sneat/assets/vendor/css/pages

### Folder: ./saas_master_template/static/sneat/assets/vendor/fonts

### Folder: ./saas_master_template/static/sneat/assets/vendor/fonts/boxicons

### Folder: ./saas_master_template/static/sneat/assets/vendor/libs

### Folder: ./saas_master_template/static/sneat/assets/vendor/libs/perfect-scrollbar

### Folder: ./saas_master_template/static/sneat/assets/vendor/libs/highlight

### Folder: ./saas_master_template/static/sneat/assets/vendor/libs/popper

### Folder: ./saas_master_template/static/sneat/assets/vendor/libs/jquery

### Folder: ./saas_master_template/static/sneat/assets/vendor/libs/masonry

### Folder: ./saas_master_template/static/sneat/assets/vendor/libs/apex-charts

### Folder: ./saas_master_template/static/sneat/assets/vendor/js

### Folder: ./saas_master_template/static/js

### Folder: ./generated_startups

### Folder: ./orchestrator
- orchestrator.py
- __init__.py

### Folder: ./tools
- memory.py
- research_tool.py
- web_search.py
- github_tool.py
- code_runner.py
- json_parser.py
- deployment_tool.py
- llm.py
- __init__.py
- file_writer.py

### Folder: ./data

### Folder: ./dashboard
- dashboard.py
- __init__.py

### Folder: ./constitution
- founder-control-room.md

### Folder: ./agents
- developer_agent.py
- github_agent.py
- growth_agent.py
- finance_agent.py
- ceo_agent.py
- cto_agent.py
- deployment_agent.py
- qa_agent.py
- __init__.py
- agent_base.py

### Folder: ./agentic-startup-factory
- PROJECT_CONTEXT.md
- build_saas_engine.py
- generate_context.py
- fill_repo.py
- README.md

### Folder: ./agentic-startup-factory/memory
- experiments.md
- decisions.md
- learnings.md

### Folder: ./agentic-startup-factory/engine
- template_renderer.py

### Folder: ./agentic-startup-factory/logs

### Folder: ./agentic-startup-factory/saas_master_template

### Folder: ./agentic-startup-factory/saas_master_template/templates

### Folder: ./agentic-startup-factory/saas_master_template/static

### Folder: ./agentic-startup-factory/saas_master_template/static/assets

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/css

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/scss

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/img

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/img/portfolio

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/img/clients

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/img/blog

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/img/testimonials

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/img/team

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/js

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/glightbox

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/glightbox/css

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/glightbox/js

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/bootstrap-icons

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/bootstrap-icons/fonts

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/swiper

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/bootstrap

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/bootstrap/css

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/bootstrap/js

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/php-email-form

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/isotope-layout

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/purecounter

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/aos

### Folder: ./agentic-startup-factory/saas_master_template/static/assets/vendor/imagesloaded

### Folder: ./agentic-startup-factory/saas_master_template/static/css

### Folder: ./agentic-startup-factory/saas_master_template/static/js

### Folder: ./agentic-startup-factory/orchestrator
- orchestrator.py
- __init__.py

### Folder: ./agentic-startup-factory/tools
- memory.py
- research_tool.py
- web_search.py
- github_tool.py
- code_runner.py
- json_parser.py
- deployment_tool.py
- llm.py
- __init__.py
- file_writer.py

### Folder: ./agentic-startup-factory/data

### Folder: ./agentic-startup-factory/dashboard
- dashboard.py
- __init__.py

### Folder: ./agentic-startup-factory/constitution
- founder-control-room.md

### Folder: ./agentic-startup-factory/agents
- developer_agent.py
- github_agent.py
- growth_agent.py
- finance_agent.py
- ceo_agent.py
- cto_agent.py
- deployment_agent.py
- qa_agent.py
- __init__.py
- agent_base.py

### Folder: ./agentic-startup-factory/projects

### Folder: ./agentic-startup-factory/projects/freelance-time-tracking-s-91022

### Folder: ./agentic-startup-factory/projects/browser-extension-for-pas-97371

### Folder: ./agentic-startup-factory/projects/browser-extension-for-pas-97370

### Folder: ./agentic-startup-factory/projects/digital-asset-management--31497
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-powered-seo-audit-tool-91934

### Folder: ./agentic-startup-factory/projects/ai-driven-influencer-mark-29026
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-powered-video-editor-f-29543
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/jobhunt
- experiments.md
- roadmap.md
- jobhunt.md

### Folder: ./agentic-startup-factory/projects/automated-task-delegation-92248

### Folder: ./agentic-startup-factory/projects/browser-extension-for-pas-97372

### Folder: ./agentic-startup-factory/projects/ai-driven-sales-forecasti-40221
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-driven-chatbot-builder-30799
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-driven-website-content-22139
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-powered-virtual-event--89544

### Folder: ./agentic-startup-factory/projects/ai-powered

### Folder: ./agentic-startup-factory/projects/ai-driven-lead-generation-44405
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/browser-extension-for-pas-97374

### Folder: ./agentic-startup-factory/projects/ai-driven-website-securit-41821
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-powered-content-calend-27956
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/freelance-contract-genera-26414
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-powered-content-summar-22731
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/freelancer-contract-manager-1773332564

### Folder: ./agentic-startup-factory/projects/virtual-event-planner-41346
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/virtual-event-promotion-p-42363
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/freelance-time-tracking-s-23324
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-powered-website-speed-optimization-to-1773331021

### Folder: ./agentic-startup-factory/projects/ai-driven-website-personalization-tool-1773339123
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-driven-email-marketing-26138
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-powered-employee-onboa-31120
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/freelance-contract-genera-27575
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/mindpal
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-powered-email-newslett-39328
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/browser-extension-for-pas-97375

### Folder: ./agentic-startup-factory/projects/dreamweaver
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/virtual-event-promotion-p-42565
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/automated-social-media-co-43231
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/browser-extension-for-pas-97334

### Folder: ./agentic-startup-factory/projects/ai-powered-video-transcription-service-1773332163
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-driven-website-content-21933
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/browser-extension-for-pas-97250

### Folder: ./agentic-startup-factory/projects/e-commerce-product-recomm-40468
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-driven-customer-feedba-30006
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/browser-extension-for-pas-97389

### Folder: ./agentic-startup-factory/projects/ai-powered-content-genera-40682
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/browser-extension-for-pas-97394

### Folder: ./agentic-startup-factory/projects/ai-driven-influencer-mark-29144
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/e-commerce-product-review-42104
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/browser-extension-for-pas-97339

### Folder: ./agentic-startup-factory/projects/startup
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/ai-powered-content-calend-27712
- app.py
- README.md

### Folder: ./agentic-startup-factory/projects/website-performance-optim-23475
- app.py
- README.md

---

## File Contents (truncated)


### ./build_saas_engine.py

```python
import os

def write_file(path, content):
    directory = os.path.dirname(path)
    if directory != "":
        os.makedirs(directory, exist_ok=True)
    with open(path, "w") as f:
        f.write(content)

print("🚀 Building Master SaaS Engine...")

# Create folders
folders = [
    "saas_master_template/templates",
    "saas_master_template/static/css",
    "saas_master_template/static/js",
    "engine",
    "generated_startups"
]

for folder in folders:
    os.makedirs(folder, exist_ok=True)

# -----------------------------
# base.html
# -----------------------------
base_html = """
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ data.product_name }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="../static/css/style.css">
</head>
<body class="bg-black text-white">
    {% block content %}
    {% endblock %}
    <script src="../static/js/main.js"></script>
</body>
</html>
"""

write_file("saas_master_template/templates/base.html", base_html)

# -----------------------------
# index.html
# -----------------------------
index_html = """
{% extends "base.html" %}
{% block content %}
    {% include "hero.html" %}
    {% include "features.html" %}
{% endblock %}
"""

write_file("saas_master_template/templates/index.html", index_html)

# -----------------------------
# hero.html
# -----------------------------
hero_html = """
<section class="py-32 text-center">
    <h1 class="text-5xl font-bold mb-6">
        {{ data.headline }}
    </h1>
    <p class="text-gray-400 mb-8">
        {{ data.subheadline }}
    </p>
    <button class="px-6 py-3 bg-indigo-600 rounded-lg">
        {{ data.cta_text }} — {{ data.price }}
    </button>
</section>
"""

write_file("saas_master_template/templates/hero.html", hero_html)

# -----------------------------
# features.html
# -----------------------------
features_html = """
<section class="py-20 max-w-6xl mx-auto grid md:grid-cols-3 gap-8">
    {% for feature in data.f
```

### ./generate_context.py

```python
import os
from datetime import datetime, UTC

PROJECT_ROOT = "."

CODEBASE_FILE = "CODEBASE_CONTEXT.md"
PROJECT_FILE = "PROJECT_CONTEXT.md"


def generate_codebase_context():

    context = []

    context.append("# Codebase Context Snapshot")
    context.append(f"Generated: {datetime.now(UTC)}\n")

    context.append("## Project Structure\n")

    python_files = []

    for root, dirs, files in os.walk(PROJECT_ROOT):

        if ".git" in root or root.startswith("./projects") or "__pycache__" in root:
            continue

        context.append(f"\n### Folder: {root}")

        for file in files:

            if file.endswith(".py") or file.endswith(".md"):
                context.append(f"- {file}")

            if file.endswith(".py"):
                python_files.append(os.path.join(root, file))

    context.append("\n---\n")
    context.append("## File Contents (truncated)\n")

    for path in python_files:

        context.append(f"\n### {path}\n")

        try:
            with open(path, "r", encoding="utf-8") as f:
                content = f.read()

            context.append("```python")
            context.append(content[:2000])
            context.append("```")

        except:
            context.append("Could not read file")

    with open(CODEBASE_FILE, "w", encoding="utf-8") as f:
        f.write("\n".join(context))


def generate_project_context():

    agents = []
    folders = []

    for root, dirs, files in os.walk(PROJECT_ROOT):

        if ".git" in root or root.startswith("./projects") or "__pycache__" in root:
            continue

        for file in files:

            if file.endswith("_agent.py"):
                agents.append(file.replace(".py", ""))

        for d in dirs:
            folders.append(d)

    context = []

    context.append("# Project Context\n")

    context.append("## Architecture\n")
    context.append("Dashboard → Orchestrator → Agents → Tools → Deployment\n")

    context.append("## Agents\n")

    for a in agent
```

### ./fill_repo.py

```python
import os

files = {

"agents/agent_base.py": '''
class AgentBase:
    def __init__(self, name):
        self.name = name

    def think(self, task):
        print(f"[{self.name}] thinking about: {task}")
        return f"{self.name} suggests action for: {task}"

    def execute(self, task):
        print(f"[{self.name}] executing task: {task}")
        return f"{self.name} completed: {task}"
''',

"agents/ceo_agent.py": '''
from agents.agent_base import AgentBase

class CEOAgent(AgentBase):
    def __init__(self):
        super().__init__("CEO Agent")

    def generate_idea(self):
        return self.think("Generate startup idea")
''',

"agents/cto_agent.py": '''
from agents.agent_base import AgentBase

class CTOAgent(AgentBase):
    def __init__(self):
        super().__init__("CTO Agent")

    def design_architecture(self, idea):
        return self.think(f"Design architecture for {idea}")
''',

"agents/developer_agent.py": '''
from agents.agent_base import AgentBase

class DeveloperAgent(AgentBase):
    def __init__(self):
        super().__init__("Developer Agent")

    def build_feature(self, architecture):
        return self.execute(f"Build MVP for {architecture}")
''',

"agents/qa_agent.py": '''
from agents.agent_base import AgentBase

class QAAgent(AgentBase):
    def __init__(self):
        super().__init__("QA Agent")

    def test_product(self, build):
        return self.execute(f"Testing {build}")
''',

"agents/growth_agent.py": '''
from agents.agent_base import AgentBase

class GrowthAgent(AgentBase):
    def __init__(self):
        super().__init__("Growth Agent")

    def launch_marketing(self, product):
        return self.execute(f"Launch marketing for {product}")
''',

"agents/finance_agent.py": '''
from agents.agent_base import AgentBase

class FinanceAgent(AgentBase):
    def __init__(self):
        super().__init__("Finance Agent")

    def track_revenue(self, product):
        return self.execute(f"Tracking revenue for {product}")
''',

"age
```

### ./engine/template_renderer.py

```python

from jinja2 import Environment, FileSystemLoader
import json
import os

def render_startup(config_path, output_dir):
    with open(config_path) as f:
        data = json.load(f)

    env = Environment(loader=FileSystemLoader("saas_master_template/templates"))
    template = env.get_template("index.html")

    html = template.render(data=data)

    os.makedirs(output_dir, exist_ok=True)

    with open(f"{output_dir}/index.html", "w") as f:
        f.write(html)

    print("Startup generated:", output_dir)

```

### ./saas_master_template/app.py

```python
from flask import Flask, render_template, redirect
from flask import url_for

PRODUCT_NAME = "AI Resume Builder"

app = Flask(__name__)
app.secret_key = "secret123"


@app.route("/")
def landing():
    return render_template(
        "index.html",
        product_name=PRODUCT_NAME,
        product_tagline="Build ATS-ready resumes instantly",
        product_headline="Create Perfect Resumes With AI",
        product_description="Our AI analyzes job descriptions and builds optimized resumes.",

        product_meta_description="AI powered resume builder",
        product_keywords="AI resume, ATS resume builder",

        product_about_title="Smart Resume Optimization",
        product_about_description="Our AI improves your resume for recruiters.",

        feature_1_title="AI Resume Analysis",
        feature_1_description="Upload resume and get instant feedback",

        feature_2_title="ATS Optimization",
        feature_2_description="Ensure recruiters see your resume",

        feature_3_title="One Click Export",
        feature_3_description="Download recruiter-ready resumes",

        plan_1_name="Free",
        plan_2_name="Starter",
        plan_2_price="9",
        plan_3_name="Pro",
        plan_3_price="29",
        plan_4_name="Business",
        plan_4_price="49",

        testimonial_1_name="Sarah",
        testimonial_1_text="This tool doubled my interview calls!",

        testimonial_2_name="David",
        testimonial_2_text="Best AI resume optimizer I’ve used.",

        contact_email="support@example.com"
    )



@app.route("/dashboard")
def dashboard():
    return render_template("dashboard.html", product_name=PRODUCT_NAME)

@app.route("/logout")
def logout():
    return redirect("/")

if __name__ == "__main__":
    app.run(debug=True)
```

### ./orchestrator/orchestrator.py

```python
from agents.ceo_agent import CEOAgent
from agents.cto_agent import CTOAgent
from agents.developer_agent import DeveloperAgent
from agents.qa_agent import QAAgent
from agents.deployment_agent import DeploymentAgent
from agents.growth_agent import GrowthAgent
from agents.finance_agent import FinanceAgent
from agents.github_agent import GitHubAgent
from tools.memory import add_entry
import subprocess
import os
from tools.code_runner import run_app

class Orchestrator:

    def __init__(self):

        self.ceo = CEOAgent()
        self.cto = CTOAgent()
        self.dev = DeveloperAgent()
        self.qa = QAAgent()
        self.deploy = DeploymentAgent()
        self.growth = GrowthAgent()
        self.finance = FinanceAgent()
        self.github = GitHubAgent()

    # 1. Add 'idea_id=None' here. Keep your 'idea' name exactly as is.
    
    def run_startup_cycle(self, idea, idea_id=None): # New

        print("\n==============================")
        print("🚀 STARTUP GENERATION STARTED")
        print("==============================")

        print(idea)
        arch = self.safe_run(
            "CTO Agent",
            self.cto.design_architecture,
            idea
        )

        print("[Developer Agent] building MVP...")
        project_path = self.safe_run(
            "Developer Agent",
            self.dev.build_mvp,
            idea,
            arch
        )

        if not project_path:
            print("Developer failed — aborting startup cycle.")
            return
        print(project_path)
        
        project_name = os.path.basename(project_path)
        print("REPO NAME BEING CREATED:", project_name)

         
        # -------- TEMPLATE VALIDATION --------

        print("\n[QA CHECK] Validating Flask template usage...")

        app_file = os.path.join(project_path, "app.py")

        try:

            if not os.path.exists(app_file):

                print("[QA CHECK] app.py not found, skipping template validation")

            else:


```

### ./orchestrator/__init__.py

```python

```

### ./tools/memory.py

```python
import json
import os

MEMORY_FOLDER = "memory"


def load_memory(file):

    path = os.path.join(MEMORY_FOLDER, file)

    if not os.path.exists(path):
        return []

    if os.path.getsize(path) == 0:
        return []

    try:
        with open(path, "r") as f:
            return json.load(f)
    except json.JSONDecodeError:
        return []


def save_memory(file, data):

    path = os.path.join(MEMORY_FOLDER, file)

    with open(path, "w") as f:
        json.dump(data, f, indent=2)


def add_entry(file, entry):

    data = load_memory(file)

    data.append(entry)

    # keep last 100 startups only
    data = data[-100:]

    save_memory(file, data)
```

### ./tools/research_tool.py

```python
def market_research(topic):
    return f"Researching {topic}"
```

### ./tools/web_search.py

```python
def web_search(query):
    return f"Searching web for {query}"
```

### ./tools/github_tool.py

```python
def push_code(repo):
    return f"Pushing code to {repo}"
```

### ./tools/code_runner.py

```python
import subprocess

def run_app(project_path):

    try:
        result = subprocess.run(
            ["python", "app.py"],
            cwd=project_path,
            capture_output=True,
            text=True,
            timeout=3
        )

        return True, result.stdout

    except subprocess.TimeoutExpired as e:
        # Flask app started successfully
        return True, "App running"

    except Exception as e:
        return False, str(e)
```

### ./tools/json_parser.py

```python
import json
import re


def extract_json(text):

    try:
        return json.loads(text)
    except:
        pass

    match = re.search(r"\{[\s\S]*\}", text)

    if match:
        try:
            return json.loads(match.group())
        except:
            pass

    return None
```

### ./tools/deployment_tool.py

```python
def deploy_app(name):
    return f"Deploying {name}"
```

### ./tools/llm.py

```python
from dotenv import load_dotenv
import os
from groq import Groq

# Load environment variables
load_dotenv()

# Get API key
api_key = os.getenv("GROQ_API_KEY")

if not api_key:
    raise ValueError("GROQ_API_KEY not found. Check your .env file.")

# Initialize Groq client
client = Groq(api_key=api_key)


def generate(prompt):

    chat_completion = client.chat.completions.create(
        model="llama-3.1-8b-instant",
        messages=[{"role": "user", "content": prompt}],
        temperature=0.7
    )

    return chat_completion.choices[0].message.content
```

### ./tools/__init__.py

```python

```

### ./tools/file_writer.py

```python
import os


def write_file(path, content):

    os.makedirs(os.path.dirname(path), exist_ok=True)

    with open(path, "w", encoding="utf-8") as f:
        f.write(content)

    print(f"[FileWriter] Created {path}")
```

### ./dashboard/dashboard.py

```python
from flask import Flask, jsonify
from orchestrator.orchestrator import Orchestrator
import json
import os
import threading  # <--- Step 1: Add this import

app = Flask(__name__)
orch = Orchestrator()

@app.route("/")
def home():
    return "Founder Control Room Dashboard Running"

@app.route("/generate_idea")
def generate_idea():

    idea = orch.ceo.generate_idea()

    return "Idea generated. Go to /ideas to approve."

@app.route("/generate_batch")
def generate_batch():

    ideas = orch.ceo.generate_multiple_ideas(20)

    return f"{len(ideas)} ideas generated. Visit /ideas to review."
    
@app.route("/ideas")
def show_ideas():

    if not os.path.exists("data/ideas.json"):
        return "No ideas yet. Generate one at /generate_idea"

    with open("data/ideas.json") as f:
        ideas = json.load(f)

    html = "<h1>Startup Ideas</h1>"

    
    
    for idea in ideas:
        # Check if a URL was saved by the Deployment Agent
        url_display = f"<br><b>URL:</b> <a href='{idea['url']}' target='_blank'>{idea['url']}</a>" if idea.get('url') else ""
        
        html += f"""
        <div style='border:1px solid #ccc;padding:10px;margin:10px;border-radius:10px;'>
        <h3>Idea #{idea['id']}</h3>
        <pre>{idea['idea']}</pre>
        Score: {idea.get('score', 'N/A')} <br>
        Status: <b>{idea['status']}</b> {url_display} <br>
        <a href="/approve/{idea['id']}">Approve & Deploy</a>
        </div>
        """

    return html

@app.route("/approve/<int:idea_id>")
def approve_idea(idea_id):
    with open("data/ideas.json") as f:
        ideas = json.load(f)

    selected_idea = None
    for idea in ideas:
        if idea["id"] == idea_id:
            # Step 1: Mark as approved
            idea["status"] = "approved"
            selected_idea = idea["idea"]
            break

    if selected_idea:
        # Step 2: Save the "Approved" status to the file
        with open("data/ideas.json", "w") as f:
            json.dump(ideas, f, indent=2)

 
```

### ./dashboard/__init__.py

```python

```

### ./agents/developer_agent.py

```python
from agents.agent_base import AgentBase
from tools.file_writer import write_file
from tools.code_runner import run_app
import os
import re
import json
import time
import shutil

class DeveloperAgent(AgentBase):

    def __init__(self):
        super().__init__("Developer Agent")

   

    def extract_project_name(self, idea):

        # If idea is dictionary (new CEO agent format)
        if isinstance(idea, dict):

            name = idea.get("name", "startup")

            # create slug
            slug = re.sub(r"[^a-zA-Z0-9]+", "-", name.lower()).strip("-")

            # limit length for GitHub repo safety
            slug = slug[:25]

            return f"{slug}-{int(time.time())%100000}"

        # If idea is text (older format)
        for line in idea.splitlines():

            if "name" in line.lower():

                words = re.findall(r"[A-Za-z0-9_-]+", line)

                if len(words) >= 2:
                    return words[1].lower()

        words = re.findall(r"[A-Za-z]+", idea)

        if words:
            return words[0].lower()

        return "startup"

    def detect_dependencies(self, code):

        deps = set()

        if "flask_sqlalchemy" in code.lower():
            deps.add("Flask-SQLAlchemy==3.0.5")

        if "flask_login" in code.lower():
            deps.add("Flask-Login==0.6.3")

        if "requests" in code:
            deps.add("requests==2.31.0")

        if "pandas" in code:
            deps.add("pandas==2.2.2")

        return deps


   

    def copy_template(self, project_dir):
        template_path = "saas_master_template"

        if not os.path.exists(template_path):
            raise Exception("SaaS template not found")

        shutil.copytree(template_path, project_dir, dirs_exist_ok=True)

    def build_mvp(self, idea, architecture=None):

        print("DEBUG IDEA TEXT:")
        print(json.dumps(idea, indent=2) if isinstance(idea, dict) else idea)

        project_name = self.extract_project_name(idea)

    
```

### ./agents/github_agent.py

```python
from github import Github
import os
import subprocess


class GitHubAgent:

    def __init__(self):
        token = os.getenv("GH_PAT")
        self.github = Github(token)
        self.user = self.github.get_user()

    def create_repo_and_push(self, project_name):

        print("[GitHub Agent] preparing repository...")

        try:
            repo = self.user.get_repo(project_name)
            print("[GitHub Agent] repo already exists")
        except:
            repo = self.user.create_repo(project_name, private=False)
            print("[GitHub Agent] repository created")

        project_path = f"projects/{project_name}"
        os.makedirs(project_path, exist_ok=True)

        subprocess.run(["git", "init"], cwd=project_path)
        subprocess.run(["git", "add", "."], cwd=project_path)
        subprocess.run(["git", "commit", "-m", "initial commit"], cwd=project_path)

        repo_url = repo.clone_url

        subprocess.run(["git", "remote", "remove", "origin"], cwd=project_path, stderr=subprocess.DEVNULL)
        token = os.getenv("GH_PAT")

        remote_url = f"https://{token}@github.com/{self.user.login}/{project_name}.git"

        subprocess.run(["git", "remote", "add", "origin", remote_url], cwd=project_path)

        subprocess.run(["git", "branch", "-M", "main"], cwd=project_path)
        subprocess.run(["git", "push", "-u", "origin", "main"], cwd=project_path)

        return repo.html_url
```

### ./agents/growth_agent.py

```python
from agents.agent_base import AgentBase

class GrowthAgent:

    def launch_marketing(self, build):

        return """
Marketing Plan

• Launch ProductHunt
• Post on LinkedIn
• Share in Reddit communities
• Create landing page
"""
```

### ./agents/finance_agent.py

```python
from agents.agent_base import AgentBase

class FinanceAgent:

    def track_revenue(self, build):

        return """
Revenue Tracking

• Track subscriptions
• Monitor MRR
• Calculate CAC
• Track conversion rate
"""
```

### ./agents/ceo_agent.py

```python
from agents.agent_base import AgentBase
from tools.memory import load_memory
from tools.json_parser import extract_json
import json
import os
import re


class CEOAgent(AgentBase):

    def __init__(self):
        super().__init__("CEO Agent")

    def generate_idea(self):

        ideas_file = "data/ideas.json"

        if not os.path.exists(ideas_file):
            with open(ideas_file, "w") as f:
                json.dump([], f)

        with open(ideas_file, "r") as f:
            ideas = json.load(f)

        past_startups = load_memory("startups.json")

        past_startup_names = [
            s.get("name")
            for s in past_startups
            if isinstance(s, dict)
        ]

        previous_ideas = [
            i.get("idea", {}).get("name")
            for i in ideas
            if isinstance(i.get("idea"), dict)
        ]

        prompt = f"""
        Generate one innovative startup idea.

        Avoid repeating or being similar to these existing ideas:
        {previous_ideas}

        Also avoid startups that were already built and deployed:
        {past_startup_names}

        Return ONLY valid JSON in this format:

        {{
        "name": "Startup Name",
        "description": "Short description",
        "market": "Target users",
        "revenue_model": "How it makes money"
        }}
        """

        response = self.think(prompt)

        idea = extract_json(response)

        if not idea:
            print("CEO Agent: Invalid JSON response")
            return None

        #score = 5

        next_id = len(ideas) + 1
        idea_entry = {
            "id": next_id,
            "idea": idea,
            "name": idea.get("name"),
            "description": idea.get("description"),
            "score": self.score_locally(idea),
            "status": "pending"
        }

        ideas.append(idea_entry)

        with open(ideas_file, "w") as f:
            json.dump(ideas, f, indent=2)

        return idea_entry

    def genera
```

### ./agents/cto_agent.py

```python
from agents.agent_base import AgentBase
from tools.json_parser import extract_json
import json


class CTOAgent(AgentBase):

    def __init__(self):
        super().__init__("CTO Agent")

    def design_architecture(self, idea):

        prompt = f"""
        You are a senior software architect.

        Design a simple micro-SaaS architecture.

        Startup Idea:
        {idea}

        Return ONLY valid JSON in this format:

        {{
          "backend": "Flask",
          "database": "SQLite",
          "models": ["User"],
          "routes": ["/", "/signup", "/login", "/dashboard"],
          "pages": ["index.html", "signup.html", "login.html", "dashboard.html"],
          "features": ["user authentication", "core feature of the startup"]
        }}

        Do NOT include explanations.
        Do NOT include markdown.
        """

        response = self.think(prompt)

        architecture = extract_json(response)

        if not architecture:
            print("[CTO Agent] Invalid architecture JSON")
            return None

        return architecture
```

### ./agents/deployment_agent.py

```python
from agents.agent_base import AgentBase
import subprocess
import os
import time
import re

class DeploymentAgent(AgentBase):
    def __init__(self):
        super().__init__("Deployment Agent")

    def deploy(self, build_message, idea_id=None): # New
        print("[Deployment Agent] Starting deployment pipeline...")
        try:
            path = build_message
            project_dir = os.path.basename(path)

            if not os.path.exists(path):
                return "Deployment failed: project folder not found"

            # 1. Initialize Railway Project
            print(f"[Deployment Agent] Initializing: {project_dir}")
            # We capture output to ensure it finishes before moving to link
            subprocess.run(["railway", "init", "--name", project_dir], cwd=path, capture_output=True)

            # 2. Link Local Folder (The "No-Argument" Fix)
            print(f"[Deployment Agent] Linking project context...")
            # Running 'railway link' without the project name argument 
            # tells it to use the project we just initialized in this directory.
            subprocess.run(["railway", "link"], cwd=path, check=False, capture_output=True)

            # 3. Deploy/Upload Code (The Non-Interactive Fix)
            print("[Deployment Agent] Running 'railway up'...")
            # Adding --detach stops the CLI from streaming logs, 
            # which often prevents the "select service" hang.
            subprocess.run(["railway", "up", "--detach"], cwd=path, check=False)

            # 4. Stability Wait (Increased slightly for domain generation)
            print("[Deployment Agent] Waiting 12s for URL generation...")
            time.sleep(12)

            # 5. Generate / Fetch Domain
            print("[Deployment Agent] Fetching public URL...")
            domain_process = subprocess.run(
                ["railway", "domain"],
                cwd=path,
                capture_output=True,
                text=True
            )

   
```

### ./agents/qa_agent.py

```python
from tools.code_runner import run_app
from tools.llm import generate
import os

class QAAgent:

    def test_product(self, project_path):

        print("[QA Agent] Running application tests...")

        success, output = run_app(project_path)

        if success:
            print("[QA Agent] App started successfully")
            return True

        print("[QA Agent] Error detected:")
        print(output)

        return output
```

### ./agents/__init__.py

```python

```

### ./agents/agent_base.py

```python
from tools.llm import generate

class AgentBase:

    def __init__(self, name):
        self.name = name

    def think(self, task):

        prompt = f"""
        You are {self.name} in an AI startup factory.

        Task:
        {task}

        Return ONLY the requested output.
        Do not include explanations.
        """

        response = generate(prompt)

        print(f"[{self.name}] AI response:")
        print(response)

        return response

    def execute(self, task):

        prompt = f"""
        You are {self.name} responsible for executing tasks.

        Task:
        {task}

        Explain the execution steps clearly.
        """

        response = generate(prompt)

        print(f"[{self.name}] execution plan:")
        print(response)

        return response
```

### ./agentic-startup-factory/build_saas_engine.py

```python
import os

def write_file(path, content):
    directory = os.path.dirname(path)
    if directory != "":
        os.makedirs(directory, exist_ok=True)
    with open(path, "w") as f:
        f.write(content)

print("🚀 Building Master SaaS Engine...")

# Create folders
folders = [
    "saas_master_template/templates",
    "saas_master_template/static/css",
    "saas_master_template/static/js",
    "engine",
    "generated_startups"
]

for folder in folders:
    os.makedirs(folder, exist_ok=True)

# -----------------------------
# base.html
# -----------------------------
base_html = """
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ data.product_name }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="../static/css/style.css">
</head>
<body class="bg-black text-white">
    {% block content %}
    {% endblock %}
    <script src="../static/js/main.js"></script>
</body>
</html>
"""

write_file("saas_master_template/templates/base.html", base_html)

# -----------------------------
# index.html
# -----------------------------
index_html = """
{% extends "base.html" %}
{% block content %}
    {% include "hero.html" %}
    {% include "features.html" %}
{% endblock %}
"""

write_file("saas_master_template/templates/index.html", index_html)

# -----------------------------
# hero.html
# -----------------------------
hero_html = """
<section class="py-32 text-center">
    <h1 class="text-5xl font-bold mb-6">
        {{ data.headline }}
    </h1>
    <p class="text-gray-400 mb-8">
        {{ data.subheadline }}
    </p>
    <button class="px-6 py-3 bg-indigo-600 rounded-lg">
        {{ data.cta_text }} — {{ data.price }}
    </button>
</section>
"""

write_file("saas_master_template/templates/hero.html", hero_html)

# -----------------------------
# features.html
# -----------------------------
features_html = """
<section class="py-20 max-w-6xl mx-auto grid md:grid-cols-3 gap-8">
    {% for feature in data.f
```

### ./agentic-startup-factory/generate_context.py

```python
import os
from datetime import datetime, UTC

PROJECT_ROOT = "."

CODEBASE_FILE = "CODEBASE_CONTEXT.md"
PROJECT_FILE = "PROJECT_CONTEXT.md"


def generate_codebase_context():

    context = []

    context.append("# Codebase Context Snapshot")
    context.append(f"Generated: {datetime.now(UTC)}\n")

    context.append("## Project Structure\n")

    python_files = []

    for root, dirs, files in os.walk(PROJECT_ROOT):

        if ".git" in root or root.startswith("./projects") or "__pycache__" in root:
            continue

        context.append(f"\n### Folder: {root}")

        for file in files:

            if file.endswith(".py") or file.endswith(".md"):
                context.append(f"- {file}")

            if file.endswith(".py"):
                python_files.append(os.path.join(root, file))

    context.append("\n---\n")
    context.append("## File Contents (truncated)\n")

    for path in python_files:

        context.append(f"\n### {path}\n")

        try:
            with open(path, "r", encoding="utf-8") as f:
                content = f.read()

            context.append("```python")
            context.append(content[:2000])
            context.append("```")

        except:
            context.append("Could not read file")

    with open(CODEBASE_FILE, "w", encoding="utf-8") as f:
        f.write("\n".join(context))


def generate_project_context():

    agents = []
    folders = []

    for root, dirs, files in os.walk(PROJECT_ROOT):

        if ".git" in root or root.startswith("./projects") or "__pycache__" in root:
            continue

        for file in files:

            if file.endswith("_agent.py"):
                agents.append(file.replace(".py", ""))

        for d in dirs:
            folders.append(d)

    context = []

    context.append("# Project Context\n")

    context.append("## Architecture\n")
    context.append("Dashboard → Orchestrator → Agents → Tools → Deployment\n")

    context.append("## Agents\n")

    for a in agent
```

### ./agentic-startup-factory/fill_repo.py

```python
import os

files = {

"agents/agent_base.py": '''
class AgentBase:
    def __init__(self, name):
        self.name = name

    def think(self, task):
        print(f"[{self.name}] thinking about: {task}")
        return f"{self.name} suggests action for: {task}"

    def execute(self, task):
        print(f"[{self.name}] executing task: {task}")
        return f"{self.name} completed: {task}"
''',

"agents/ceo_agent.py": '''
from agents.agent_base import AgentBase

class CEOAgent(AgentBase):
    def __init__(self):
        super().__init__("CEO Agent")

    def generate_idea(self):
        return self.think("Generate startup idea")
''',

"agents/cto_agent.py": '''
from agents.agent_base import AgentBase

class CTOAgent(AgentBase):
    def __init__(self):
        super().__init__("CTO Agent")

    def design_architecture(self, idea):
        return self.think(f"Design architecture for {idea}")
''',

"agents/developer_agent.py": '''
from agents.agent_base import AgentBase

class DeveloperAgent(AgentBase):
    def __init__(self):
        super().__init__("Developer Agent")

    def build_feature(self, architecture):
        return self.execute(f"Build MVP for {architecture}")
''',

"agents/qa_agent.py": '''
from agents.agent_base import AgentBase

class QAAgent(AgentBase):
    def __init__(self):
        super().__init__("QA Agent")

    def test_product(self, build):
        return self.execute(f"Testing {build}")
''',

"agents/growth_agent.py": '''
from agents.agent_base import AgentBase

class GrowthAgent(AgentBase):
    def __init__(self):
        super().__init__("Growth Agent")

    def launch_marketing(self, product):
        return self.execute(f"Launch marketing for {product}")
''',

"agents/finance_agent.py": '''
from agents.agent_base import AgentBase

class FinanceAgent(AgentBase):
    def __init__(self):
        super().__init__("Finance Agent")

    def track_revenue(self, product):
        return self.execute(f"Tracking revenue for {product}")
''',

"age
```

### ./agentic-startup-factory/engine/template_renderer.py

```python

from jinja2 import Environment, FileSystemLoader
import json
import os

def render_startup(config_path, output_dir):
    with open(config_path) as f:
        data = json.load(f)

    env = Environment(loader=FileSystemLoader("saas_master_template/templates"))
    template = env.get_template("index.html")

    html = template.render(data=data)

    os.makedirs(output_dir, exist_ok=True)

    with open(f"{output_dir}/index.html", "w") as f:
        f.write(html)

    print("Startup generated:", output_dir)

```

### ./agentic-startup-factory/orchestrator/orchestrator.py

```python
from agents.ceo_agent import CEOAgent
from agents.cto_agent import CTOAgent
from agents.developer_agent import DeveloperAgent
from agents.qa_agent import QAAgent
from agents.deployment_agent import DeploymentAgent
from agents.growth_agent import GrowthAgent
from agents.finance_agent import FinanceAgent
from agents.github_agent import GitHubAgent
from tools.memory import add_entry
import subprocess
import os

class Orchestrator:

    def __init__(self):

        self.ceo = CEOAgent()
        self.cto = CTOAgent()
        self.dev = DeveloperAgent()
        self.qa = QAAgent()
        self.deploy = DeploymentAgent()
        self.growth = GrowthAgent()
        self.finance = FinanceAgent()
        self.github = GitHubAgent()

    # 1. Add 'idea_id=None' here. Keep your 'idea' name exactly as is.
    
    def run_startup_cycle(self, idea, idea_id=None): # New

        print("\n==============================")
        print("🚀 STARTUP GENERATION STARTED")
        print("==============================")

        print(idea)
        arch = self.safe_run(
            "CTO Agent",
            self.cto.design_architecture,
            idea
        )

        print("[Developer Agent] building MVP...")
        project_path = self.safe_run(
            "Developer Agent",
            self.dev.build_mvp,
            idea,
            arch
        )

        if not project_path:
            print("Developer failed — aborting startup cycle.")
            return
        print(project_path)

        
        project_name = os.path.basename(project_path)
        print("REPO NAME BEING CREATED:", project_name)

        repo = self.safe_run(
            "GitHub Agent",
            self.github.create_repo_and_push,
            project_name
        )
        print(repo)

        test = self.safe_run(
            "QA Agent",
            self.qa.test_product,
            project_path
        )

        # 2. Update this line to pass the idea_id to your deployment agent
        deploy = self.s
```

### ./agentic-startup-factory/orchestrator/__init__.py

```python

```

### ./agentic-startup-factory/tools/memory.py

```python
import json
import os

MEMORY_FOLDER = "memory"


def load_memory(file):

    path = os.path.join(MEMORY_FOLDER, file)

    if not os.path.exists(path):
        return []

    if os.path.getsize(path) == 0:
        return []

    try:
        with open(path, "r") as f:
            return json.load(f)
    except json.JSONDecodeError:
        return []


def save_memory(file, data):

    path = os.path.join(MEMORY_FOLDER, file)

    with open(path, "w") as f:
        json.dump(data, f, indent=2)


def add_entry(file, entry):

    data = load_memory(file)

    data.append(entry)

    # keep last 100 startups only
    data = data[-100:]

    save_memory(file, data)
```

### ./agentic-startup-factory/tools/research_tool.py

```python
def market_research(topic):
    return f"Researching {topic}"
```

### ./agentic-startup-factory/tools/web_search.py

```python
def web_search(query):
    return f"Searching web for {query}"
```

### ./agentic-startup-factory/tools/github_tool.py

```python
def push_code(repo):
    return f"Pushing code to {repo}"
```

### ./agentic-startup-factory/tools/code_runner.py

```python
import subprocess

def run_app(project_path):

    try:
        result = subprocess.run(
            ["python", "app.py"],
            cwd=project_path,
            capture_output=True,
            text=True,
            timeout=3
        )

        return True, result.stdout

    except subprocess.TimeoutExpired as e:
        # Flask app started successfully
        return True, "App running"

    except Exception as e:
        return False, str(e)
```

### ./agentic-startup-factory/tools/json_parser.py

```python
import json
import re


def extract_json(text):

    try:
        return json.loads(text)
    except:
        pass

    match = re.search(r"\{[\s\S]*\}", text)

    if match:
        try:
            return json.loads(match.group())
        except:
            pass

    return None
```

### ./agentic-startup-factory/tools/deployment_tool.py

```python
def deploy_app(name):
    return f"Deploying {name}"
```

### ./agentic-startup-factory/tools/llm.py

```python
from dotenv import load_dotenv
import os
from groq import Groq

# Load environment variables
load_dotenv()

# Get API key
api_key = os.getenv("GROQ_API_KEY")

if not api_key:
    raise ValueError("GROQ_API_KEY not found. Check your .env file.")

# Initialize Groq client
client = Groq(api_key=api_key)


def generate(prompt):

    chat_completion = client.chat.completions.create(
        model="llama-3.1-8b-instant",
        messages=[{"role": "user", "content": prompt}],
        temperature=0.7
    )

    return chat_completion.choices[0].message.content
```

### ./agentic-startup-factory/tools/__init__.py

```python

```

### ./agentic-startup-factory/tools/file_writer.py

```python
import os


def write_file(path, content):

    os.makedirs(os.path.dirname(path), exist_ok=True)

    with open(path, "w", encoding="utf-8") as f:
        f.write(content)

    print(f"[FileWriter] Created {path}")
```

### ./agentic-startup-factory/dashboard/dashboard.py

```python
from flask import Flask, jsonify
from orchestrator.orchestrator import Orchestrator
import json
import os
import threading  # <--- Step 1: Add this import

app = Flask(__name__)
orch = Orchestrator()

@app.route("/")
def home():
    return "Founder Control Room Dashboard Running"

@app.route("/generate_idea")
def generate_idea():

    idea = orch.ceo.generate_idea()

    return "Idea generated. Go to /ideas to approve."

@app.route("/generate_batch")
def generate_batch():

    ideas = orch.ceo.generate_multiple_ideas(20)

    return f"{len(ideas)} ideas generated. Visit /ideas to review."
    
@app.route("/ideas")
def show_ideas():

    if not os.path.exists("data/ideas.json"):
        return "No ideas yet. Generate one at /generate_idea"

    with open("data/ideas.json") as f:
        ideas = json.load(f)

    html = "<h1>Startup Ideas</h1>"

    
    
    for idea in ideas:
        # Check if a URL was saved by the Deployment Agent
        url_display = f"<br><b>URL:</b> <a href='{idea['url']}' target='_blank'>{idea['url']}</a>" if idea.get('url') else ""
        
        html += f"""
        <div style='border:1px solid #ccc;padding:10px;margin:10px;border-radius:10px;'>
        <h3>Idea #{idea['id']}</h3>
        <pre>{idea['idea']}</pre>
        Score: {idea.get('score', 'N/A')} <br>
        Status: <b>{idea['status']}</b> {url_display} <br>
        <a href="/approve/{idea['id']}">Approve & Deploy</a>
        </div>
        """

    return html

@app.route("/approve/<int:idea_id>")
def approve_idea(idea_id):
    with open("data/ideas.json") as f:
        ideas = json.load(f)

    selected_idea = None
    for idea in ideas:
        if idea["id"] == idea_id:
            # Step 1: Mark as approved
            idea["status"] = "approved"
            selected_idea = idea["idea"]
            break

    if selected_idea:
        # Step 2: Save the "Approved" status to the file
        with open("data/ideas.json", "w") as f:
            json.dump(ideas, f, indent=2)

 
```

### ./agentic-startup-factory/dashboard/__init__.py

```python

```

### ./agentic-startup-factory/agents/developer_agent.py

```python
from agents.agent_base import AgentBase
from tools.file_writer import write_file
from tools.code_runner import run_app
import os
import re
import json
import time

class DeveloperAgent(AgentBase):

    def __init__(self):
        super().__init__("Developer Agent")

   

    def extract_project_name(self, idea):

        # If idea is dictionary (new CEO agent format)
        if isinstance(idea, dict):

            name = idea.get("name", "startup")

            # create slug
            slug = re.sub(r"[^a-zA-Z0-9]+", "-", name.lower()).strip("-")

            # limit length for GitHub repo safety
            slug = slug[:25]

            return f"{slug}-{int(time.time())%100000}"

        # If idea is text (older format)
        for line in idea.splitlines():

            if "name" in line.lower():

                words = re.findall(r"[A-Za-z0-9_-]+", line)

                if len(words) >= 2:
                    return words[1].lower()

        words = re.findall(r"[A-Za-z]+", idea)

        if words:
            return words[0].lower()

        return "startup"

    def detect_dependencies(self, code):

        deps = set()

        if "flask_sqlalchemy" in code:
            deps.add("Flask-SQLAlchemy==3.0.5")

        if "flask_login" in code:
            deps.add("Flask-Login==0.6.3")

        if "requests" in code:
            deps.add("requests==2.31.0")

        if "pandas" in code:
            deps.add("pandas==2.2.2")

        return deps



    def build_mvp(self, idea, architecture):

        print("DEBUG IDEA TEXT:")
        print(json.dumps(idea, indent=2) if isinstance(idea, dict) else idea)

        project_name = self.extract_project_name(idea)

        print("EXTRACTED PROJECT NAME:", project_name)

        project_dir = f"projects/{project_name}"
        os.makedirs(project_dir, exist_ok=True)

        prompt = f"""
        arch_text = ""

        if architecture:
            arch_text = f"""

        Architecture Specification:
        {json.dum
```

### ./agentic-startup-factory/agents/github_agent.py

```python
from github import Github
import os
import subprocess


class GitHubAgent:

    def __init__(self):
        token = os.getenv("GH_PAT")
        self.github = Github(token)
        self.user = self.github.get_user()

    def create_repo_and_push(self, project_name):

        print("[GitHub Agent] preparing repository...")

        try:
            repo = self.user.get_repo(project_name)
            print("[GitHub Agent] repo already exists")
        except:
            repo = self.user.create_repo(project_name, private=False)
            print("[GitHub Agent] repository created")

        project_path = f"projects/{project_name}"
        os.makedirs(project_path, exist_ok=True)

        subprocess.run(["git", "init"], cwd=project_path)
        subprocess.run(["git", "add", "."], cwd=project_path)
        subprocess.run(["git", "commit", "-m", "initial commit"], cwd=project_path)

        repo_url = repo.clone_url

        subprocess.run(["git", "remote", "remove", "origin"], cwd=project_path, stderr=subprocess.DEVNULL)
        token = os.getenv("GH_PAT")

        remote_url = f"https://{token}@github.com/{self.user.login}/{project_name}.git"

        subprocess.run(["git", "remote", "add", "origin", remote_url], cwd=project_path)

        subprocess.run(["git", "branch", "-M", "main"], cwd=project_path)
        subprocess.run(["git", "push", "-u", "origin", "main"], cwd=project_path)

        return repo.html_url
```

### ./agentic-startup-factory/agents/growth_agent.py

```python
from agents.agent_base import AgentBase

class GrowthAgent:

    def launch_marketing(self, build):

        return """
Marketing Plan

• Launch ProductHunt
• Post on LinkedIn
• Share in Reddit communities
• Create landing page
"""
```

### ./agentic-startup-factory/agents/finance_agent.py

```python
from agents.agent_base import AgentBase

class FinanceAgent:

    def track_revenue(self, build):

        return """
Revenue Tracking

• Track subscriptions
• Monitor MRR
• Calculate CAC
• Track conversion rate
"""
```

### ./agentic-startup-factory/agents/ceo_agent.py

```python
from agents.agent_base import AgentBase
from tools.memory import load_memory
from tools.json_parser import extract_json
import json
import os
import re


class CEOAgent(AgentBase):

    def __init__(self):
        super().__init__("CEO Agent")

    def generate_idea(self):

        ideas_file = "data/ideas.json"

        if not os.path.exists(ideas_file):
            with open(ideas_file, "w") as f:
                json.dump([], f)

        with open(ideas_file, "r") as f:
            ideas = json.load(f)

        past_startups = load_memory("startups.json")

        past_startup_names = [
            s.get("name")
            for s in past_startups
            if isinstance(s, dict)
        ]

        previous_ideas = [
            i.get("idea", {}).get("name")
            for i in ideas
            if isinstance(i.get("idea"), dict)
        ]

        prompt = f"""
        Generate one innovative startup idea.

        Avoid repeating or being similar to these existing ideas:
        {previous_ideas}

        Also avoid startups that were already built and deployed:
        {past_startup_names}

        Return ONLY valid JSON in this format:

        {{
        "name": "Startup Name",
        "description": "Short description",
        "market": "Target users",
        "revenue_model": "How it makes money"
        }}
        """

        response = self.think(prompt)

        idea = extract_json(response)

        if not idea:
            print("CEO Agent: Invalid JSON response")
            return None

        #score = 5

        next_id = len(ideas) + 1
        idea_entry = {
            "id": next_id,
            "idea": idea,
            "name": idea.get("name"),
            "description": idea.get("description"),
            "score": self.score_locally(idea),
            "status": "pending"
        }

        ideas.append(idea_entry)

        with open(ideas_file, "w") as f:
            json.dump(ideas, f, indent=2)

        return idea_entry

    def genera
```

### ./agentic-startup-factory/agents/cto_agent.py

```python
import json
from tools.json_parser import extract_json

def design_architecture(self, idea):

    prompt = f"""
    You are a senior software architect.

    Design a simple micro-SaaS architecture.

    Startup Idea:
    {idea}

    Return ONLY valid JSON in this format:

    {{
      "backend": "Flask",
      "database": "SQLite",
      "models": ["User"],
      "routes": ["/", "/signup", "/login", "/dashboard"],
      "pages": ["index.html", "signup.html", "login.html", "dashboard.html"],
      "features": ["user authentication", "core feature of the startup"]
    }}

    Do NOT include explanations.
    Do NOT include markdown.
    """

    response = self.think(prompt)

    architecture = extract_json(response)

    if not architecture:
        print("[CTO Agent] Invalid architecture JSON")
        return None

    return architecture
```

### ./agentic-startup-factory/agents/deployment_agent.py

```python
from agents.agent_base import AgentBase
import subprocess
import os
import time
import re

class DeploymentAgent(AgentBase):
    def __init__(self):
        super().__init__("Deployment Agent")

    def deploy(self, build_message, idea_id=None): # New
        print("[Deployment Agent] Starting deployment pipeline...")
        try:
            path = build_message
            project_dir = os.path.basename(path)

            if not os.path.exists(path):
                return "Deployment failed: project folder not found"

            # 1. Initialize Railway Project
            print(f"[Deployment Agent] Initializing: {project_dir}")
            # We capture output to ensure it finishes before moving to link
            subprocess.run(["railway", "init", "--name", project_dir], cwd=path, capture_output=True)

            # 2. Link Local Folder (The "No-Argument" Fix)
            print(f"[Deployment Agent] Linking project context...")
            # Running 'railway link' without the project name argument 
            # tells it to use the project we just initialized in this directory.
            subprocess.run(["railway", "link"], cwd=path, check=False, capture_output=True)

            # 3. Deploy/Upload Code (The Non-Interactive Fix)
            print("[Deployment Agent] Running 'railway up'...")
            # Adding --detach stops the CLI from streaming logs, 
            # which often prevents the "select service" hang.
            subprocess.run(["railway", "up", "--detach"], cwd=path, check=False)

            # 4. Stability Wait (Increased slightly for domain generation)
            print("[Deployment Agent] Waiting 12s for URL generation...")
            time.sleep(12)

            # 5. Generate / Fetch Domain
            print("[Deployment Agent] Fetching public URL...")
            domain_process = subprocess.run(
                ["railway", "domain"],
                cwd=path,
                capture_output=True,
                text=True
            )

   
```

### ./agentic-startup-factory/agents/qa_agent.py

```python
from agents.agent_base import AgentBase

class QAAgent:

    def test_product(self, build):

        return """
QA Checklist

• Run the application
• Test API endpoints
• Check error handling
• Validate responses
"""
```

### ./agentic-startup-factory/agents/__init__.py

```python

```

### ./agentic-startup-factory/agents/agent_base.py

```python
from tools.llm import generate

class AgentBase:

    def __init__(self, name):
        self.name = name

    def think(self, task):

        prompt = f"""
        You are {self.name} in an AI startup factory.

        Task:
        {task}

        Provide a concise professional response.
        """

        response = generate(prompt)

        print(f"[{self.name}] AI response:")
        print(response)

        return response

    def execute(self, task):

        prompt = f"""
        You are {self.name} responsible for executing tasks.

        Task:
        {task}

        Explain the execution steps clearly.
        """

        response = generate(prompt)

        print(f"[{self.name}] execution plan:")
        print(response)

        return response
```

### ./agentic-startup-factory/projects/digital-asset-management--31497/app.py

```python

import os
from flask import Flask

app = Flask(__name__)

port = int(os.environ.get("PORT", 5000))

@app.route('/')
def index():
    return 'Welcome to Digital Asset Management Tool'

if __name__ == '__main__':
    app.run(host="0.0.0.0", port=port)


```

### ./agentic-startup-factory/projects/ai-driven-influencer-mark-29026/app.py

```python

import os

from flask import Flask

app = Flask(__name__)

port = int(os.environ.get("PORT", 5000))

@app.route('/', methods=['GET'])
def home():
    return 'Welcome to AI-driven Influencer Marketing Platform'

@app.route('/influencer', methods=['GET'])
def influencer():
    return 'Identify and manage influencer marketing campaigns'

if __name__ == '__main__':
    app.run(host="0.0.0.0", port=port)


```

### ./agentic-startup-factory/projects/ai-powered-video-editor-f-29543/app.py

```python

import os

from flask import Flask

app = Flask(__name__)

port = int(os.environ.get("PORT", 5000))

@app.route("/")
def hello_world():
    return "Welcome to AI-powered Video Editor for Social Media!"

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=port)


```

### ./agentic-startup-factory/projects/ai-driven-sales-forecasti-40221/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    return "Welcome to AI-driven Sales Forecasting Tool! Predict sales based on historical data and market trends."

if __name__ == "__main__":
    app.run(debug=True)


```

### ./agentic-startup-factory/projects/ai-driven-chatbot-builder-30799/app.py

```python

import os
from flask import Flask, jsonify

app = Flask(__name__)

@app.route('/health')
def health():
    """Return a message to indicate the server is running."""
    return 'AI-driven Chatbot Builder for Small Businesses is running!'

@app.route('/chatbot-builder')
def chatbot_builder():
    """Return a message to indicate the chatbot builder is available."""
    return 'Create conversational chatbots for small businesses.'

if __name__ == '__main__':
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port)


```

### ./agentic-startup-factory/projects/ai-driven-website-content-22139/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def home():
    """Return plain text from the homepage route"""
    return "Welcome to AI-driven Website Content Analyzer"

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/ai-driven-lead-generation-44405/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    return "Welcome to AI-driven Lead Generation Tool. Identify and qualify leads using AI-powered lead generation tools."

if __name__ == "__main__":
    import os
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/ai-driven-website-securit-41821/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def home():
    return "Welcome to the AI-driven Website Security Scanner"

if __name__ == "__main__":
    import os
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/ai-powered-content-calend-27956/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    return "Welcome to AI-powered Content Calendar Tool! Plan, schedule, and optimize content for social media"

if __name__ == "__main__":
    app.run()


```

### ./agentic-startup-factory/projects/freelance-contract-genera-26414/app.py

```python

import os
from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    return "Welcome to Freelance Contract Generator. Automatically generate freelance contracts based on project details."

port = int(os.environ.get("PORT", 5000))
app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/ai-powered-content-summar-22731/app.py

```python

import os
from flask import Flask

app = Flask(__name__)

@app.route("/")
def home():
    """Return a plain text message"""
    return "Welcome to the AI-powered Content Summarization Tool"

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/virtual-event-planner-41346/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route('/')
def index():
    return 'Welcome to Virtual Event Planner!'

if __name__ == '__main__':
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/virtual-event-promotion-p-42363/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def home():
    """Return a plain text message."""
    return "Welcome to Virtual Event Promotion Platform!"

if __name__ == "__main__":
    import os
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/freelance-time-tracking-s-23324/app.py

```python

import os
from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    """Return plain text from the homepage route"""
    return "Welcome to Freelance Time Tracking Software! Track your freelance work hours and generate invoices automatically."

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/ai-driven-website-personalization-tool-1773339123/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    return "Welcome to AI-driven Website Personalization Tool"

if __name__ == "__main__":
    app.run()


```

### ./agentic-startup-factory/projects/ai-driven-email-marketing-26138/app.py

```python

import os
from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    return "Welcome to AI-driven Email Marketing Automation"

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/ai-powered-employee-onboa-31120/app.py

```python

import os

from flask import Flask

app = Flask(__name__)

port = int(os.environ.get("PORT", 5000))

@app.route('/')
def home():
    return 'Welcome to AI-powered Employee Onboarding Platform!'

@app.route('/onboarding')
def onboarding():
    return 'Automate employee onboarding and training with our AI-powered platform.'

if __name__ == '__main__':
    app.run(host="0.0.0.0", port=port)


```

### ./agentic-startup-factory/projects/freelance-contract-genera-27575/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    """Return homepage text."""
    return "Welcome to Freelance Contract Generator. This is a simple MVP to generate freelance contracts based on project details."

if __name__ == "__main__":
    app.run()


```

### ./agentic-startup-factory/projects/mindpal/app.py

```python
```python
from flask import Flask, request, jsonify
from flask_cors import CORS
import nltk
from nltk.sentiment import SentimentIntensityAnalyzer
import pickle
from sklearn.metrics.pairwise import cosine_similarity
from sklearn.feature_extraction.text import TfidfVectorizer

app = Flask(__name__)
CORS(app)

# Load pre-trained models
sia = SentimentIntensityAnalyzer()

# Define routes
@app.route('/analyze', methods=['POST'])
def analyze_emotions():
    """Analyze user input and return emotional intelligence analysis"""
    user_input = request.json['text']
    sentiment_scores = sia.polarity_scores(user_input)
    return jsonify(sentiment_scores)

@app.route('/recommend', methods=['POST'])
def recommend_resources():
    """Provide personalized recommendations based on user input"""
    user_input = request.json['text']
    # Load pre-trained recommendation model
    with open('recommendation_model.pkl', 'rb') as f:
        model = pickle.load(f)
    # Generate recommendations
    recommendations = model.predict(user_input)
    return jsonify(recommendations)

@app.route('/mood_track', methods=['POST'])
def track_mood():
    """Track user mood and provide journaling functionality"""
    user_input = request.json['text']
    # Save user input to database or file
    with open('mood_journal.txt', 'a') as f:
        f.write(user_input + '\n')
    return jsonify({'message': 'Mood tracked successfully'})

@app.route('/therapists', methods=['GET'])
def get_therapists():
    """Provide access to certified therapists and support groups"""
    # Load therapist database or API
    therapists = [{'name': 'Therapist 1', 'specialty': 'Anxiety'}, {'name': 'Therapist 2', 'specialty': 'Depression'}]
    return jsonify(therapists)

if __name__ == '__main__':
    app.run(debug=True)
```

```

### ./agentic-startup-factory/projects/ai-powered-email-newslett-39328/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route('/', methods=['GET'])
def homepage():
    """Return the homepage content"""
    return "Welcome to AI-powered Email Newsletter Creator. Automatically generate engaging email newsletters."

if __name__ == '__main__':
    app.run()


```

### ./agentic-startup-factory/projects/dreamweaver/app.py

```python
**DreamWeaver MVP**

```python
from flask import Flask, jsonify, request
from flask_cors import CORS

app = Flask(__name__)
CORS(app)

# Sample in-memory data store (replace with a database in production)
users = {}
sessions = {}

# API Endpoints
@app.route('/register', methods=['POST'])
def register():
    """Register a new user"""
    data = request.json
    user_id = data['user_id']
    users[user_id] = {
        'name': data['name'],
        'email': data['email']
    }
    return jsonify({'message': 'User registered successfully'}), 201

@app.route('/login', methods=['POST'])
def login():
    """Login an existing user"""
    data = request.json
    user_id = data['user_id']
    if user_id in users:
        return jsonify({'message': 'User logged in successfully'}), 200
    return jsonify({'message': 'Invalid user ID'}), 401

@app.route('/sessions', methods=['POST'])
def create_session():
    """Create a new meditation session"""
    data = request.json
    session_id = data['session_id']
    sessions[session_id] = {
        'user_id': data['user_id'],
        'session_type': data['session_type'],
        'duration': data['duration']
    }
    return jsonify({'message': 'Session created successfully'}), 201

@app.route('/sessions/<session_id>', methods=['GET'])
def get_session(session_id):
    """Retrieve a meditation session"""
    if session_id in sessions:
        return jsonify(sessions[session_id]), 200
    return jsonify({'message': 'Session not found'}), 404

if __name__ == '__main__':
    app.run(debug=True)
```

```

### ./agentic-startup-factory/projects/virtual-event-promotion-p-42565/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    return "Virtual Event Promotion Platform - Promoting virtual events and tracking attendee engagement"

if __name__ == "__main__":
    import os
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/automated-social-media-co-43231/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def home():
    """Return plain text from the homepage route"""
    return "Welcome to Automated Social Media Content Generator"

if __name__ == "__main__":
    import os
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/ai-powered-video-transcription-service-1773332163/app.py

```python

from flask import Flask, render_template
import os
import socket

app = Flask(__name__)

@app.route("/")
def index():
    """Homepage route"""
    return render_template("index.html")

if __name__ == "__main__":
    app.run(debug=True, threaded=True)

```

### ./agentic-startup-factory/projects/ai-driven-website-content-21933/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    return "Welcome to AI-driven Website Content Analyzer"

if __name__ == "__main__":
    import os
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/e-commerce-product-recomm-40468/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def home():
    return "Welcome to E-commerce Product Recommendation Tool"

if __name__ == "__main__":
    app.run()


```

### ./agentic-startup-factory/projects/ai-driven-customer-feedba-30006/app.py

```python

import os
from flask import Flask

app = Flask(__name__)

port = int(os.environ.get("PORT", 5000))

@app.route('/analyze', methods=['GET'])
def analyze_feedback():
    return "AI-driven Customer Feedback Analytics: Analyze customer feedback and sentiment analysis"

if __name__ == '__main__':
    app.run(host="0.0.0.0", port=port)


```

### ./agentic-startup-factory/projects/ai-powered-content-genera-40682/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def home():
    return "Welcome to the AI-powered Content Generation Tool. Automatically generate high-quality content for blogs and social media."

if __name__ == "__main__":
    app.run(debug=True)


```

### ./agentic-startup-factory/projects/ai-driven-influencer-mark-29144/app.py

```python

import os
from flask import Flask

app = Flask(__name__)

port = int(os.environ.get("PORT", 5000))

@app.route("/")
def home():
    return "Welcome to AI-driven Influencer Marketing Platform!"

@app.route("/influencer")
def influencer():
    return "Identify and manage influencer marketing campaigns"

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=port)


```

### ./agentic-startup-factory/projects/e-commerce-product-review-42104/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    return "Welcome to E-commerce Product Review Analysis Tool"

if __name__ == "__main__":
    import os
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```

### ./agentic-startup-factory/projects/startup/app.py

```python

```python
from flask import Flask, jsonify, request
from flask_sqlalchemy import SQLAlchemy
from flask_marshmallow import Marshmallow
from flask_cors import CORS

app = Flask(__name__)
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///dreamweaver.db'
db = SQLAlchemy(app)
ma = Marshmallow(app)
CORS(app)

class User(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    name = db.Column(db.String(100), nullable=False)
    email = db.Column(db.String(100), unique=True, nullable=False)

class UserSchema(ma.SQLAlchemyAutoSchema):
    class Meta:
        model = User
        load_instance = True

# Initialize database
with app.app_context():
    db.create_all()

# Define routes
@app.route('/users', methods=['GET'])
def get_users():
    users = User.query.all()
    user_schema = UserSchema(many=True)
    return jsonify(user_schema.dump(users))

@app.route('/users', methods=['POST'])
def create_user():
    new_user = User(name=request.json['name'], email=request.json['email'])
    db.session.add(new_user)
    db.session.commit()
    return jsonify({'message': 'New user created!'})

if __name__ == '__main__':
    app.run(debug=True)
```

```

### ./agentic-startup-factory/projects/ai-powered-content-calend-27712/app.py

```python

from flask import Flask

app = Flask(__name__)

@app.route('/')
def homepage():
    return 'Welcome to the AI-powered Content Calendar Tool!'

if __name__ == '__main__':
    app.run()


```

### ./agentic-startup-factory/projects/website-performance-optim-23475/app.py

```python

import os
from flask import Flask

app = Flask(__name__)

@app.route("/")
def homepage():
    return "Welcome to Website Performance Optimization Tool"

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port, debug=False)


```