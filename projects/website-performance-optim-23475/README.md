
# Website Performance Optimization Tool

## Setup

1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `python app.py`

## Cloud Deployment

1. Set environment variable `PORT` to the desired port number
2. Use Railway to deploy the app

Note: Make sure to install the required dependencies and run the app as specified in the README before deploying to the cloud.